const host = 'http://api.giphy.com';
const randomEndpoint = '/v1/gifs/random';
const apiKey = 'H098CFAR8lxoTJxEUKPwVdS0a97CTRCT';

export default class Giphy {
  getRandomGif(callback) {
    fetch(host + randomEndpoint, {
      
    })
  }
}